import { Box, Flex } from "@chakra-ui/react";
import React from "react";

export default function RelayerStatusBanner() {
  return (
    <Flex
      alignItems="center"
      justifyContent="center"
      height="var(--banner-height)"
      borderBottom="var(--border)"
      borderColor="border"
    >
      RelayerStatusBanner
    </Flex>
  );
}
